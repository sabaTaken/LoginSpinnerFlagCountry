<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="mySpinnerStyle" parent="@android:style/Widget.Spinner">
        <item name="android:divider">#01579B</item>
        <item name="android:dividerHeight">1dp</item>
    </style>

    <style name="colorHint" parent="Theme.AppCompat.NoActionBar">
        <item name="android:textColorHint">@color/color_command_button_state_not_confirm</item>
    </style>
</resources>
